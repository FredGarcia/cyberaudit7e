##############################################
# CyberAudit7E — Profile PROD
# PostgreSQL, logs réduits, JPA optimisé
# Activé via : SPRING_PROFILES_ACTIVE=prod
##############################################

# M3+ : configuration PostgreSQL
# spring:
#   datasource:
#     url: jdbc:postgresql://${DB_HOST:db}:${DB_PORT:5432}/${DB_NAME:cyberaudit7e}
#     username: ${DB_USER:audit}
#     password: ${DB_PASSWORD:audit7e}
#   jpa:
#     show-sql: false
#     hibernate:
#       ddl-auto: validate
#     properties:
#       hibernate:
#         dialect: org.hibernate.dialect.PostgreSQLDialect
#         jdbc.batch_size: 25
#         order_inserts: true
#   flyway:
#     enabled: true

logging:
  level:
    com.cyberaudit7e: INFO
    com.cyberaudit7e.service.cycle: INFO
    org.springframework: WARN
